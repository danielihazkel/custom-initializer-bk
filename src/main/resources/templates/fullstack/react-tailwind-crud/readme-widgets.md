# widgets

Composite UI blocks that combine features and entities into a self-contained
section of a page (e.g. a header, a sidebar). Empty by default — add slices here
as the app grows. Import only from `features`, `entities`, `shared`.
