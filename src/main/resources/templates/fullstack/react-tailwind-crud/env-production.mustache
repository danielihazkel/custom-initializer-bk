# Production build: leave empty so the app calls same-origin /api/... behind the nginx reverse
# proxy (see nginx/nginx.conf). Set an absolute origin here only when the API lives elsewhere.
VITE_API_BASE_URL=
