# pages

Route-level components — one slice per entity (`<entity>/ui/<Entity>Page.tsx`).
A page composes widgets, features, entities, and shared building blocks into a
full screen. Import only from layers below (`widgets`, `features`, `entities`,
`shared`).
