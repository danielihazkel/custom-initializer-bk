{{#hasDateRange}}
import { useState } from 'react'
{{/hasDateRange}}
import { {{#usesBar}}BreakdownCard, {{/usesBar}}{{#usesKpi}}KpiTile, {{/usesKpi}}{{#hasDateRange}}PeriodSelect, {{/hasDateRange}}{{#usesLine}}TrendCard, {{/usesLine}}{{#usesRecent}}RecentList, {{/usesRecent}}} from '@shared/ui/widgets'
{{#usesStatsHelpers}}
import { {{#usesRangeParams}}rangeParams, {{/usesRangeParams}}{{#usesStatsQuery}}statsQuery, {{/usesStatsQuery}}type Period } from '@shared/ui/stats'
{{/usesStatsHelpers}}
{{#usesT}}
import { t } from '@shared/i18n'
{{/usesT}}
{{#labelImports}}
import { {{labelsRefs}} } from '@entities/{{entityNameKebab}}'
{{/labelImports}}
{{#needsNavigate}}

interface Props {
  onNavigate: (view: string, arg?: string, query?: Record<string, string>) => void
}
{{/needsNavigate}}

export function {{PageName}}Screen({{#needsNavigate}}{ onNavigate }: Props{{/needsNavigate}}) {
{{#hasDateRange}}
  // The period every widget with a date column is limited to.
  const [period, setPeriod] = useState<Period>('{{dateRangeDefault}}')
{{/hasDateRange}}
  return (
    <div className="mx-auto max-w-6xl p-4 md:p-8">
{{^hasDateRange}}
      <div className="mb-6">
        <h1 className="text-2xl font-semibold tracking-tight text-fg">{ {{pageTitleExpr}} }</h1>
{{#hasPageDescription}}
        <p className="mt-1 max-w-2xl text-sm text-muted">{ {{pageDescriptionExpr}} }</p>
{{/hasPageDescription}}
      </div>
{{/hasDateRange}}
{{#hasDateRange}}
      <div className="mb-6 flex flex-wrap items-end justify-between gap-4">
        <div>
          <h1 className="text-2xl font-semibold tracking-tight text-fg">{ {{pageTitleExpr}} }</h1>
{{#hasPageDescription}}
          <p className="mt-1 max-w-2xl text-sm text-muted">{ {{pageDescriptionExpr}} }</p>
{{/hasPageDescription}}
        </div>
        <PeriodSelect value={period} onChange={setPeriod} />
      </div>
{{/hasDateRange}}
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
{{#widgets}}
{{#widgetIsKpi}}
        <KpiTile
          title={ {{titleExpr}} }
          path="{{path}}"
{{#hasAgg}}
          agg="{{agg}}"
          field="{{aggField}}"
{{/hasAgg}}
{{#hasParams}}
          params={ {{paramsExpr}} }
{{/hasParams}}
{{#hasSpanClass}}
          className="{{spanClass}}"
{{/hasSpanClass}}
{{#hasTarget}}
          onOpen={() => onNavigate('{{targetPageId}}')}
{{/hasTarget}}
        />
{{/widgetIsKpi}}
{{#widgetIsLine}}
        <TrendCard
          title={ {{titleExpr}} }
          path="{{path}}"
          on="{{groupBy}}"
          bucket="{{bucket}}"
{{#hasAgg}}
          agg="{{agg}}"
          field="{{aggField}}"
{{/hasAgg}}
{{#hasParams}}
          params={ {{paramsExpr}} }
{{/hasParams}}
{{#hasSpanClass}}
          className="{{spanClass}}"
{{/hasSpanClass}}
{{#hasTarget}}
          onOpen={() => onNavigate('{{targetPageId}}')}
{{/hasTarget}}
        />
{{/widgetIsLine}}
{{#widgetIsBar}}
        <BreakdownCard
          title={ {{titleExpr}} }
          path="{{path}}"
          field="{{groupBy}}"
{{#hasAgg}}
          agg="{{agg}}"
          valueField="{{aggField}}"
{{/hasAgg}}
{{#hasLabels}}
          labels={ {{labelsRef}} }
{{/hasLabels}}
{{#hasParams}}
          params={ {{paramsExpr}} }
{{/hasParams}}
{{#hasSpanClass}}
          className="{{spanClass}}"
{{/hasSpanClass}}
{{#hasTarget}}
          onOpen={() => onNavigate('{{targetPageId}}')}
{{/hasTarget}}
        />
{{/widgetIsBar}}
{{#widgetIsRecent}}
        <RecentList
          title={ {{titleExpr}} }
          path="{{path}}"
          sortField="{{sortField}}"
{{#hasKeyField}}
          keyField="{{keyField}}"
{{/hasKeyField}}
          displayField="{{displayField}}"
          limit={ {{limit}} }
{{#hasParams}}
          params={ {{paramsExpr}} }
{{/hasParams}}
{{#hasSpanClass}}
          className="{{spanClass}}"
{{/hasSpanClass}}
{{#hasRecordPage}}
          onOpenRow={r => onNavigate('{{recordPageId}}', String(r['{{recordPk}}']))}
{{/hasRecordPage}}
{{#hasTarget}}
          onOpen={() => onNavigate('{{targetPageId}}')}
{{/hasTarget}}
        />
{{/widgetIsRecent}}
{{/widgets}}
      </div>
    </div>
  )
}
