import { {{#usesBar}}BreakdownCard, {{/usesBar}}{{#usesKpi}}KpiTile, {{/usesKpi}}{{#usesRecent}}RecentList, {{/usesRecent}}} from '@shared/ui/widgets'
{{#usesT}}
import { t } from '@shared/i18n'
{{/usesT}}
{{#labelImports}}
import { {{labelsRefs}} } from '@entities/{{entityNameKebab}}'
{{/labelImports}}
{{#needsNavigate}}

interface Props {
  onNavigate: (view: string) => void
}
{{/needsNavigate}}

export function {{PageName}}Screen({{#needsNavigate}}{ onNavigate }: Props{{/needsNavigate}}) {
  return (
    <div className="mx-auto max-w-6xl p-4 md:p-8">
      <div className="mb-6">
        <h1 className="text-2xl font-semibold tracking-tight text-fg">{ {{pageTitleExpr}} }</h1>
{{#hasPageDescription}}
        <p className="mt-1 max-w-2xl text-sm text-muted">{ {{pageDescriptionExpr}} }</p>
{{/hasPageDescription}}
      </div>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
{{#widgets}}
{{#widgetIsKpi}}
        <KpiTile
          title={ {{titleExpr}} }
          path="{{path}}"
{{#hasTarget}}
          onOpen={() => onNavigate('{{targetPageId}}')}
{{/hasTarget}}
        />
{{/widgetIsKpi}}
{{#widgetIsBar}}
        <div className="sm:col-span-2">
          <BreakdownCard
            title={ {{titleExpr}} }
            path="{{path}}"
            field="{{groupBy}}"
{{#hasLabels}}
            labels={ {{labelsRef}} }
{{/hasLabels}}
{{#hasTarget}}
            onOpen={() => onNavigate('{{targetPageId}}')}
{{/hasTarget}}
          />
        </div>
{{/widgetIsBar}}
{{#widgetIsRecent}}
        <div className="sm:col-span-2">
          <RecentList
            title={ {{titleExpr}} }
            path="{{path}}"
            sortField="{{sortField}}"
            displayField="{{displayField}}"
            limit={ {{limit}} }
{{#hasTarget}}
            onOpen={() => onNavigate('{{targetPageId}}')}
{{/hasTarget}}
          />
        </div>
{{/widgetIsRecent}}
{{/widgets}}
      </div>
    </div>
  )
}
