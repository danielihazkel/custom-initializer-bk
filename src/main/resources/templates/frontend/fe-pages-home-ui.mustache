const cardStyles = {
  maxWidth: 720,
  margin: '3rem auto',
  padding: '2.5rem',
  background: '#ffffff',
  border: '1px solid #e6e9ee',
  borderRadius: 16,
  boxShadow: '0 1px 3px rgba(15, 23, 42, 0.08)',
} as const;

const badgeStyles = {
  display: 'inline-block',
  padding: '0.25rem 0.75rem',
  borderRadius: 9999,
  background: '{{palette.primary}}',
  color: '#ffffff',
  fontSize: 12,
  fontWeight: 600,
  letterSpacing: '0.04em',
  textTransform: 'uppercase',
} as const;

const titleStyles = {
  margin: '1rem 0 0.5rem',
  fontSize: '1.75rem',
  letterSpacing: '-0.02em',
  color: '#0f172a',
} as const;

const mutedText = { color: '#64748b', lineHeight: 1.6 } as const;

const logoStyles = { height: 44, width: 'auto', display: 'block', marginBottom: '1.25rem' } as const;

export function HomePage() {
  return (
    <main style={cardStyles}>
      <img src="/logo.png" alt="Menora" style={logoStyles} />
      <span style={badgeStyles}>Menora</span>
      <h1 style={titleStyles}>{{appTitle}}</h1>
      <p style={mutedText}>Welcome to your new React + TypeScript + Vite + Feature-Slice Design project.</p>
      <p style={mutedText}>Edit <code>src/pages/home/ui/HomePage.tsx</code> to get started.</p>
    </main>
  );
}
